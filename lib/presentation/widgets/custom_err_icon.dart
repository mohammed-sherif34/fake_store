import 'package:flutter/material.dart';

class CustomErrIcon extends StatelessWidget {
  const CustomErrIcon({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(Icons.error_outline),
    );
  }
}
